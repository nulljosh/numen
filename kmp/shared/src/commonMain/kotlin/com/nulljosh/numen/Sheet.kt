package com.nulljosh.numen

// ponytail: parser stub. Port numen's recursive-descent expression parser
// (see ../../src in the web app) into commonMain before wiring the UI to it.
data class Card(val id: String, val expression: String, val result: Double? = null)
