node_modules
dist
.wrangler

# Xcode build output. Numen.xcodeproj IS committed (generated by xcodegen, but the
# ship workflow expects it to exist), curvely's checked-in ios/.build is the mistake
# not to copy.
ios/.build
ios/build
DerivedData
*.xcuserstate
xcuserdata
